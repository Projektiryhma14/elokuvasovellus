// src/context/AuthContext.jsx
//---------------------------------------
// Auth-katto: hallinnoi SKELETON -> GUEST -> USER -tiloja
// Tässä mallissa ei auto-login

{/*
import React from 'react'
import {
    createContext,          // Luo konteksti-objektin (globaali "kanava" arvon jakamiseen komponenttipuussa)
    useContext,             // Koukku: lukee lähimmän <Authcontext.provider>in arvon
    useEffect,
    useMemo,                // Laskee ja "muistaa" arvon vain riippuvuuksien muuttuessa -> turhien renderöintien esto
    useState
} from 'react'


// 1) Luodaan konteksti ja oma hook, jolla sitä käytetään
const AuthContext = createContext(null);
export const useAuth = () => useContext(AuthContext);
*/}